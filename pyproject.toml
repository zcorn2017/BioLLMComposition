[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "biollmcomposition"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["python-dotenv"]

[tool.setuptools.packages.find]
include = ["biollmcomposition*"]

